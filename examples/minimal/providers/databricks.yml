provider:
  cloud: azure
  connect_timeout_seconds: 10
  credential_mode: personal-access-token
  execution_mode: state-only
  host: "https://adb-placeholder.azuredatabricks.net"
  auth_type: pat
  token_env: DATABRICKS_TOKEN

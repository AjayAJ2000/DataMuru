masking:
  builtins:
    - id: partial_mask
      description: Show last four characters for strings.
      strategy: partial_mask
    - id: full_mask
      description: Replace value entirely.
      strategy: full_mask
